name: Smoke test

on:
  pull_request:
    paths:
      - "hooks/**"
      - "settings.json"

jobs:
  smoke-test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Run smoke tests
        run: python3 hooks/smoke_test.py
